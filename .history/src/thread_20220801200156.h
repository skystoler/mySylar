#ifndef __SYLAR_THREAD_H__
#define __SYLAR_THREAD_H__

//pthread__xx
//std::thread,pthread

#include <thread>
#include <functional>

namespace sylar{

class Thread{
public:

private:
    Thread(const Thread&) = delete;
    Thread(const Thread&&) = delete;
};

}

#endif
 